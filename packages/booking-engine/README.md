# @lacquer/booking-engine

Reserved for a later milestone. No runtime implementation in Milestone 1.

Pure TypeScript domain logic only. Must not import SvelteKit, UI, HTTP, database clients, or provider SDKs. Future availability and booking rules belong on the backend.
